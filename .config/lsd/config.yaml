classic: false

# Possible values: permission, user, group, size, size_value, date, name, inode
blocks:
  - size
  - date
  - name
  # - permission

date: relative

dereference: false

icons:
  when: auto
  theme: fancy
  separator: " "

indicators: false

layout: grid

recursion:
  enabled: false

size: default

sorting:
  column: name
  reverse: false
  dir-grouping: first

no-symlink: false

total-size: false

symlink-arrow: 󰁔
